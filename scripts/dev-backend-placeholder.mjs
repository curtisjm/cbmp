#!/usr/bin/env node

const lines = [
  "CBMP backend dev placeholder",
  "Convex backend behavior is deferred to the next implementation slice.",
  "Use pnpm dev:web for the Next.js app by itself, or pnpm dev for the full app path.",
];

console.log(lines.join("\n"));
